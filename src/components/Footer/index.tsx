import styles from './Footer.module.css';

export function Footer() {
  return (
    <footer className={styles.footer}>
      <p>TalentLens uses AI to provide resume feedback. Results are suggestions, not guarantees.</p>
    </footer>
  );
}

